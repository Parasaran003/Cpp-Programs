#include<iostream>
using namespace std;
class bank
{
public:
    int balance,deposit,total;
    int passbook()
    {
       b.total = b.deposit + b.balance;
       s.total = s.deposit + s.balance;

    }
};
int main()
{
    bank b,s;
    cout<<"The Balance Amount is = "<<endl;
    cin>>b.balance;
    cout<<"The Deposit Amount is = "<<endl;
    cin>>b.deposit;
    cout<<"The Total Amount = "<<s.total;
    return 0;
    cout<<"The Total Amount = "<<b.total;
    return 0;
}
